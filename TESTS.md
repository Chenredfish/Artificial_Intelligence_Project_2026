# AI Battle 測試計畫

> 使用 AI Battle 批次測試功能執行。先手一律設「隨機」以消除先手偏差。

## 核心原則

**局數決定統計信度，時限決定每局搜尋深度。**

| 時限 | 估計搜尋深度 | 適合用途 |
|------|------------|---------|
| 10s  | depth ≈ 9–10  | 功能確認、快速方向判斷 |
| 15s  | depth ≈ 10–11 | 強度比較的主力時限 |
| 30s  | depth ≈ 11–12 | 細微差距測試（如權重調整） |
| 60s  | depth ≈ 12–13 | 比賽模式模擬，平時不需要 |

各優化（LMR、NMP、平行）的相對優劣在 depth 8+ 就已穩定顯現——兩組設定之間的勝率差距方向不會因為「都只到 depth 10」而改變。

| 局數 | 能可靠偵測的最小勝率差距 |
|------|----------------------|
| 10 局 | ±20% 以上才可信 |
| 20 局 | ±15% 以上可信 |
| 30 局 | ±12% 以上可信 |
| 50 局 | ±8% 以上可信 |

**實際原則：** 縮短時限後多跑幾局，比長時限跑少局更容易看出差距。

---

## 1. 冒煙測試（功能是否正常）

驗證所有功能可以正常跑完，不崩潰。

| 項目 | 設定 |
|------|------|
| AB / UV | Minimax, d=5, NMP✓, LMR✓ |
| 先手 | 隨機 |
| 時限 | 10s |

**短版本：** 3 局（約 1 分鐘）  
**長版本：** 10 局（約 3 分鐘）

**預期結果：** 所有局正常結束，狀態列顯示「完成 N 局」，勝負分布無明顯異常（兩隊都有機會獲勝）。

---

## 2. 平行模式相容性測試（新修正驗證）

確認平行模式在有時限的情況下可以正常運作（舊版平行僅在無時限時觸發）。

| 項目 | AB | UV |
|------|----|----|
| 策略 | Minimax, d=50, 平行✓ | Minimax, d=50, 平行✓ |
| 先手 | 隨機 | |
| 時限 | 10s | |

**短版本：** 5 局（約 1 分鐘）  
**長版本：** 15 局（約 3 分鐘）

**預期結果：**
- 每局耗時接近設定時限（10s 時限每局約 10–15s），不會無限跑
- CPU 使用率明顯上升（可從工作管理員確認多核被使用）
- 勝負分布接近 50/50（對稱設定）

---

## 3. 平行 vs 循序強度測試（同時限）

確認平行在相同時限下是否比循序更強。  
核心問題：同樣時間預算，平行的多核分攤優勢是否大於失去共享 TT 的損失？

| 項目 | AB（對照） | UV（實驗） |
|------|----------|----------|
| 策略 | Minimax, d=50 | Minimax, d=50 |
| 平行 | 否 | 是 |
| NMP / LMR | ✓ | ✓ |
| 時限 | 15s | 15s |

**短版本：** 10 局（約 4 分鐘）  
**長版本：** 30 局（約 12 分鐘）

**預期結果：**
- 若平行較強：UV 勝率 > 55%
- 若差異不顯著（48–52%）：TT 損失大致抵銷多核優勢，比賽時可關閉平行
- 若循序較強：比賽一律用循序

---

## 4. LMR 強度測試

驗證 LMR 是否真的讓 AI 更強（而非只是加速）。

| 項目 | AB（無 LMR） | UV（有 LMR） |
|------|------------|------------|
| 策略 | Minimax, d=50 | Minimax, d=50 |
| LMR | 否 | 是 |
| NMP | ✓ | ✓ |
| 時限 | 15s | 15s |

**短版本：** 10 局（約 4 分鐘）  
**長版本：** 30 局（約 12 分鐘）

**預期結果：**
- UV（有 LMR）應有明顯勝率優勢（> 55%），因為相同時限內 LMR 讓 ID 多走幾層
- 若 UV 勝率 < 50%：LMR 誤剪了關鍵分支，需調整 `min-d` 或 `start` 參數（見測試 6）

---

## 5. NMP 強度測試

驗證 Null Move Pruning 是否增強 AI。

| 項目 | AB（無 NMP） | UV（有 NMP） |
|------|------------|------------|
| 策略 | Minimax, d=50 | Minimax, d=50 |
| NMP | 否 | 是 |
| LMR | ✓ | ✓ |
| 時限 | 15s | 15s |

**短版本：** 10 局（約 4 分鐘）  
**長版本：** 30 局（約 12 分鐘）

**預期結果：**
- UV（有 NMP）應有勝率優勢
- 若差距不大（48–52%）：棋子數少、殘局多，Zugzwang 較常見，NMP 效益有限屬正常

---

## 6. LMR 參數調整測試

找出最佳的 `min-d` 和 `start` 組合。每次只改一個變數，對照組固定為預設值。

### 6A — 調整 min-d（啟用 LMR 的最低深度）

每組：AB = 預設（min-d=3），UV = 實驗值，d=50，**時限 15s，20 局（約 8 分鐘）**

| UV 組合 | min-d | 預期 |
|--------|-------|------|
| 激進 | 2 | 更多縮減，可能誤剪 |
| 預設 | 3 | 基準線 |
| 保守 | 4 | 較少縮減，更安全但效益降低 |

**預期結果：** min-d=3 或 4 應優於 min-d=2；若 min-d=4 更好，更新預設值。

### 6B — 調整 start（從第幾個走法開始縮減，0-based）

每組：AB = 預設（start=3），UV = 實驗值，d=50，**時限 15s，20 局（約 8 分鐘）**

| UV 組合 | start | 預期 |
|--------|-------|------|
| 激進 | 1 | 幾乎所有走法都縮減 |
| 預設 | 3 | 基準線 |
| 保守 | 5 | 只縮減排序最後的走法 |

**預期結果：** start=3 或 5 應優於 start=1（縮減排序靠前的好走法會損失精度）。

---

## 7. 深度影響測試（無時限固定深度）

了解固定深度下，多深才真正有強度差異。用無時限模式排除時間變數。

| 項目 | AB | UV |
|------|----|----|
| 策略 | Minimax | Minimax |
| 深度 | 5 | 7 |
| 時限 | 0（無時限） | 0（無時限） |
| NMP / LMR | ✓ | ✓ |

**短版本：** 10 局（每局約 10–30s，共 5–10 分鐘）  
**長版本：** 20 局

也可測試 d=7 vs d=9，依此類推。

**預期結果：**
- depth=7 應明顯強於 depth=5（UV 勝率 > 60%）
- 若差距很小（< 10%）：表示 depth=5 在這個遊戲已接近飽和

---

## 8. 策略基準測試（強度排名確認）

確認 Minimax > Greedy > Random 的強度排名確實成立。若任一測試方向相反，表示評估函式有嚴重問題。

**測試 A：Minimax vs Random**

| 項目 | AB | UV |
|------|----|----|
| 策略 | Minimax d=5 | Random |
| 時限 | 10s | — |

**短版本：** 10 局，**預期：** AB 勝率 > 80%

**測試 B：Minimax vs Greedy**

| 項目 | AB | UV |
|------|----|----|
| 策略 | Minimax d=5 | Greedy |
| 時限 | 10s | — |

**短版本：** 10 局，**預期：** AB 勝率 > 65%

**測試 C：Greedy vs Random**

**短版本：** 10 局，**預期：** AB（Greedy）勝率 > 70%

---

## 9. 評估函式權重調整測試

⚠️ **此測試是唯一真正需要較長時限的測試**，因為權重差異細微，需要更深的搜尋才能積累成明顯的勝率差。

目前權重：A/U=10, B/V=8, c/d/w/x=4, e/f/y/z=3（僅 AI 啟發，非計分用途）

**建議先測：小棋權重從 4 調降至 3（更貼近實際計分比例 1:3）**

| 項目 | AB（原始） | UV（調整後） |
|------|----------|------------|
| 策略 | Minimax d=50 | Minimax d=50 |
| NMP / LMR | ✓ | ✓ |
| 時限 | **30s** | **30s** |

**短版本：** 20 局（約 20 分鐘）  
**長版本：** 50 局（約 50 分鐘）

**預期結果：**
- 若 UV 勝率 > 55%：調整有效，更新 `EVAL_PIECE_WEIGHTS`
- 若接近 50/50：差異不顯著，保持原設定

---

## 測試執行順序建議

```
功能驗證（快，必做）：   1 → 2
強度排名確認（快）：     8
新功能效果確認：         3 → 4 → 5      （各約 12 分鐘）
參數調整（選擇性）：     6A → 6B         （各約 8 分鐘）
深度研究（選擇性）：     7               （需要較多時間）
權重調整（最後做）：     9               （需要最多時間）
```

---

## 結果判讀標準

| 勝率差距 | 結論 |
|---------|------|
| < ±5%   | 統計雜訊，無顯著差異（30 局以下尤其不可信） |
| ±5–12%  | 小幅優勢，值得保留但非關鍵 |
| ±12–25% | 明顯優勢，確認該設定更強 |
| > ±25%  | 壓倒性優勢，幾乎可確定結論 |

> **重要：** 10 局樣本的誤差範圍約 ±20%，只能用來排除明顯錯誤方向。
> 30 局才有基本統計意義，50 局以上才能判斷細微差距（如權重調整）。
